import "@testing-library/jest-dom";
import { render } from "@testing-library/react";

describe("Running Test for slider", () => {
  /**
   * Test to check if the slider is rendered
   */
  test("Render slider", () => {
    render(<></>);
    // expect(screen.getByTestId("slider")).toBeInTheDocument();
  });
});
