import "@testing-library/jest-dom";
import { render } from "@testing-library/react";

describe("Running Test for select", () => {
  /**
   * Test to check if the select is rendered
   */
  test("Render select", () => {
    render(<></>);
    // expect(screen.getByTestId("select")).toBeInTheDocument();
  });
});
