import "@testing-library/jest-dom";
import { render } from "@testing-library/react";

describe("Running Test for switch", () => {
  /**
   * Test to check if the switch is rendered
   */
  test("Render switch", () => {
    render(<></>);
    // expect(screen.getByTestId("switch")).toBeInTheDocument();
  });
});
